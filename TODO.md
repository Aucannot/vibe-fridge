# vibe-fridge 功能完成 TODO

> 目标：把 vibe-fridge 从“Flutter 迁移中可运行版本”推进到“核心库存闭环稳定、AI/识别能力可用、可打包发布”的产品状态。
>
> 约定：这里的 TODO 指项目要完成的功能清单，不是 app 内的 TodoList 功能。

## 当前已选 UI 方向

- 当前方向：**方案 C · Warm Lifestyle**。
- Codex 执行 harness：`docs/codex-goals/warm-lifestyle-ui-refresh.md`。
- 文字版视觉规范：`docs/ui-drafts/warm-lifestyle-spec.md`。
- 设计稿文件名约定：
  - `docs/ui-drafts/warm-lifestyle-refined.png`
  - `docs/ui-drafts/warm-lifestyle-design-system.png`

Codex 做新 UI 时，不要直接吃整份 TODO；应把 `docs/codex-goals/warm-lifestyle-ui-refresh.md` 作为 goal，把本文件作为 roadmap 背景资料。

## 当前基线

- [x] 仓库处于 Kivy -> Flutter 迁移期：legacy Python/Kivy 仍在 `app/`，Flutter 新工程在 `mobile/`。
- [x] Flutter app shell 与底部导航已建立：首页、物品、添加、食谱、设置。
- [x] 本地 SQLite、默认分类、示例 Wiki、首页统计、物品目录、添加物品、Wiki 详情、库存详情、历史记录已接入。
- [x] 订单截图 VLM 识别入口、识别结果预览、批量入库、VLM 设置页已接入。
- [x] legacy SQLite -> Flutter JSON 导入通道已接入。
- [ ] Android SDK/设备与 Android/macOS 通知运行时验收仍需补齐；macOS Xcode build/test/launch 已通过，Web 页面实现截图已补齐。

## UI 工作流约定

所有 UI-heavy 功能都按下面流程走：

1. **先生成 UI 稿**：用 ChatGPT image2 生成目标页面/流程的高保真稿。
2. **沉淀设计稿**：把最终参考图放到 `docs/ui-drafts/`，命名为 `<feature>-<screen>.png`；同时在对应 PR/Issue 里贴图。
3. **再 Flutter 复现**：实现时优先复用 `AppColors`、`PageHeader`、`SectionCard`、`MetricCard`、`StatusPill` 等现有组件。
4. **截图对比验收**：PR 描述里附实现截图，并说明和 UI 稿的主要差异。

推荐 image2 prompt 模板：

```text
为 vibe-fridge 设计 <页面/流程名称> 的移动端 UI 高保真稿。
产品定位：本地优先的冰箱/家庭库存管理 app，用于管理物品、保质期、提醒、订单截图识别和食谱消耗建议。
视觉风格：Material 3、中文界面、温和浅色背景、卡片式布局、圆角、清晰层级、适合 Flutter 复现。
必须包含：<核心字段/状态/操作>。
输出：一张包含主要状态的手机界面设计稿。
```

## P0：核心库存闭环稳定

### 0.1 工程与质量基线

- [x] 本地确认 Flutter SDK 路径，并让 `cd mobile && flutter pub get` 可稳定执行：当前使用 `.tools/flutter/bin/flutter`。
- [x] 接入 CI：至少跑 `flutter analyze`、`flutter test`。
- [x] 新增基础 widget smoke；首页统计、添加表单和库存闭环由 repository/acceptance 测试覆盖。
- [x] 新增 repository 单测或集成测试：创建物品、更新数量、标记消耗、删除、恢复。
- [x] 新增 app 内自验收测试入口：设置页可运行核心库存闭环检查并清理临时数据。
- [x] 补充 README：Flutter 启动、legacy 导入、VLM 配置、常见问题。

验收标准：新 PR 默认能看到 analyze/test 结果；README 能让新开发者 10 分钟内跑起 Flutter app。

### 0.2 SQLite schema 与迁移

- [x] 为 Flutter SQLite 引入正式 migration 策略，不再只依赖 `version: 1` 初始建表。
- [x] 为未来字段预留迁移：标签、存储位置、图片附件、提醒规则、导入来源、AI 识别置信度。
- [x] 增加数据库备份/恢复前置能力，避免本地数据损坏无法回滚。
- [x] 定义数据不变量：数量必须大于 0、日期格式、状态流转、Wiki 删除策略。

验收标准：升级旧本地数据库不会丢数据；schema 变更有 migration 测试覆盖。

### 0.3 物品/批次 CRUD 完整化

- [x] 添加库存物品。
- [x] 自动创建或关联 Wiki 条目。
- [x] 库存详情页支持增减数量、标记已消耗、删除、恢复。
- [x] 编辑库存基础信息。
- [x] 批次合并/拆分策略：同名同过期日默认不自动合并，保留独立批次。
- [x] 支持存储位置：冷藏、冷冻、常温、药箱、浴室等。
- [x] 支持图片附件：手动拍照/选择图片记录包装或小票。
- [x] 支持标签：临期优先、已开封、囤货、常用、易浪费。
- [x] 支持批量操作：批量标记消耗、批量删除、批量修改分类/位置。

验收标准：用户能完成“添加 -> 查看 -> 更新数量 -> 消耗/过期归档 -> 历史查看”的闭环。

### 0.4 过期提醒与通知

- [x] 数据层已有默认提醒日期概念。
- [x] UI 支持为单个物品设置提醒开关和提前天数。
- [x] 分类/Wiki 默认提醒规则：例如牛奶提前 2 天、药品提前 30 天。
- [ ] Android 本地通知：权限引导、定时调度、点击跳转详情页（代码已接入，待 Android SDK/设备验证）。
- [ ] macOS 本地通知：权限引导、调度、点击行为（代码已接入，macOS build/native tests/launch 已通过；仍待系统授权后的投递和点击验证）。
- [x] 首页新增“今天要处理”聚合模块：今日到期、已过期、提醒到期。
- [x] 通知防重复：同一物品同一提醒类型一天只发一次。
- [x] 支持稍后提醒/忽略本次提醒。

验收标准：测试数据中 3 天内过期的物品能触发可见提醒；用户能在设置中看到提醒状态。

## P1：订单截图识别入库

### 1.1 VLM 配置与安全

- [x] 设置页支持 endpoint/model/API key 配置。
- [x] API key 改用安全存储，避免明文 shared preferences。
- [x] 配置测试按钮：验证 endpoint、model、key 是否可用。
- [x] 错误提示分类：网络错误、鉴权失败、模型返回不可解析、图片格式不支持。
- [x] 支持清空配置与恢复默认配置。

验收标准：用户配置失败时知道该改哪一项；key 不以明文形式展示和持久化。

### 1.2 识别流程 UX

- [x] 添加页支持选择订单截图。
- [x] 识别后进入预览确认，再批量添加库存。
- [x] UI 稿：`docs/ui-drafts/order-import-review.png`。
- [x] 预览页支持逐项编辑：名称、数量、单位、分类、购买日期、预测过期日、置信度。
- [x] 支持勾选/取消勾选待导入条目。
- [x] 支持低置信度高亮和“需要确认”状态。
- [x] 导入前做重复检测：同订单号/同商品/同购买日期提醒用户。
- [x] 导入完成后给出 summary：新增多少、跳过多少、需要手动处理多少。

验收标准：一张复杂订单截图识别失败也不会破坏手动添加流程；用户能在导入前修正错误。

### 1.3 识别能力增强

- [x] 保存识别原图路径或缩略图，并在导入记录里可追溯。
- [x] 支持多平台图片来源：相册、文件、相机拍摄（桌面相机暂无系统拍摄 UI 时提示改用相册/文件）。
- [x] 支持常见电商/外卖/商超订单格式的 prompt 调优。
- [x] 为识别结果建立 fixtures，覆盖中英文商品名、规格、组合套餐、退款/赠品。
- [x] 增加 fallback：VLM 不可用时允许手动批量粘贴文本解析。

验收标准：主流订单截图能稳定产出可编辑结构化结果。

## P2：食谱与消耗建议

### 2.1 食谱入口从占位变成可用

- [x] UI 稿：`docs/ui-drafts/recipes-home.png`。
- [x] 食谱首页展示“优先消耗”食材：按过期日、数量、分类排序。
- [x] 基于当前库存生成 3-5 个菜谱建议。
- [x] 菜谱详情包含：所需库存、缺失食材、步骤、预计耗时、消耗哪些临期物品。
- [x] 支持“做这道菜后扣减库存”。
- [x] 支持收藏/最近生成。

验收标准：用户能从临期物品进入菜谱建议，并把执行结果反映到库存数量。

### 2.2 AI recipe 能力

- [x] 设计 recipe prompt：输入库存、过期日期、偏好、忌口、烹饪时间。
- [x] 支持用户偏好设置：口味、饮食限制、厨具、人数。
- [x] 生成结果失败时 fallback 到规则推荐：优先消耗临期食材的简单搭配。
- [x] 对 AI 结果做结构化解析和可编辑校验。

验收标准：AI 失败时 app 仍能给出基本消耗建议。

## P3：采购与补货

> 注意：这是“采购/补货清单”功能，不是通用 TodoList。

- [x] UI 稿：`docs/ui-drafts/shopping-list.png`。
- [x] 从低库存/已消耗/常买物品生成采购建议。
- [x] 用户可手动把库存条目加入采购清单。
- [x] 采购清单支持勾选、数量、单位、备注、分类分组。
- [x] 从采购清单快速转为库存入库。
- [x] 根据历史消耗频率生成“可能快用完了”的提醒。

验收标准：用户能从“吃完/快过期/低库存”自然进入“下次买什么”，再回到入库。

## P4：数据迁移、导入导出与备份

### 4.1 legacy 迁移完善

- [x] legacy SQLite export script。
- [x] Flutter asset import hook。
- [x] 导入前预览：即将导入多少分类、Wiki、库存、标签。
- [x] 导入冲突处理策略：按 id、按名称、按时间戳。
- [x] 导入日志：成功、跳过、失败原因。
- [x] 一键清空 demo 数据后导入真实数据。
- [x] 导入后数据健康检查：孤儿 Wiki、负数数量、非法日期。

验收标准：旧数据迁移可重复执行，且不会默默覆盖用户真实数据。

### 4.2 备份与导出

- [x] JSON 导出：完整保留分类、Wiki、库存、标签、设置。
- [x] CSV 导出：适合人工查看库存和历史记录。
- [x] JSON 导入：支持从备份恢复。
- [x] 本地备份提醒：首次导入/大量修改后提示备份。

验收标准：用户可以把数据导出、重装 app、再导入恢复。

## P5：信息架构与视觉统一

### 5.1 Design system

- [x] UI 稿：`docs/ui-drafts/design-system.png`。
- [x] 固化颜色、字号、圆角、阴影、间距 token。
- [x] 整理通用组件：空状态、错误状态、加载状态、确认弹窗、底部 sheet、状态 pill、统计卡片。
- [x] 所有页面统一最大宽度、列表间距、按钮层级。
- [x] 增加 dark mode 预留或明确暂不支持：当前锁定 light theme，保留 `AppTheme.supportsDarkMode` 开关。

验收标准：新增页面不再临时堆样式，主要页面视觉一致。

### 5.2 页面级 UI 重做顺序

- [x] 首页 Dashboard：临期提醒、统计、今日处理、分类分布（UI 稿和 Web 实现截图已补）。
- [x] 添加物品：手动添加 + 订单识别双入口（UI 稿和 Web 实现截图已补）。
- [x] 识别预览：低置信度修正、批量导入（UI 稿和 Web 实现截图已补）。
- [x] 物品目录：搜索、分类、库存批次、历史记录（UI 稿和 Web 实现截图已补）。
- [x] 库存详情：数量操作、提醒、图片、标签（UI 稿和 Web 实现截图已补）。
- [x] Wiki 详情：默认单位、默认保质期、存储位置、历史批次（UI 稿和 Web 实现截图已补）。
- [x] 食谱建议：首页、详情、扣减库存（UI 稿和 Web 实现截图已补）。
- [x] 设置：数据、VLM、通知、隐私/关于（UI 稿和 Web 实现截图已补；工程迁移状态不展示在 app 内）。

验收标准：每个页面都有 image2 UI 稿、Flutter 实现截图、差异说明。

## P6：平台能力与发布

### 6.1 Android

- [x] 相机/相册权限。
- [ ] 本地通知权限和定时任务（代码已接入，待 Android SDK/设备验证）。
- [x] Android release build 配置。
- [x] App icon、splash screen、应用名。
- [x] 隐私说明：本地数据、API key、订单截图发送到用户配置的 VLM endpoint。

### 6.2 macOS

- [x] 文件选择、图片读取权限确认。
- [ ] 本地通知权限（代码已接入，macOS build/native tests/launch 已通过；待系统授权后的投递和点击验证）。
- [x] macOS 打包与签名流程。
- [x] 菜单栏/窗口尺寸体验优化。

验收标准：Android/macOS 都能完成核心流程，且权限失败时有可理解提示。

## P7：可靠性与可观测性

- [x] 错误边界：数据库打开失败、导入失败、识别失败、权限失败。
- [x] 用户可复制错误详情，便于 issue 反馈。
- [x] 关键操作加入确认或撤销：删除、批量导入、批量消耗。
- [x] 添加 sample 数据重置入口，仅 debug/dev 可见。
- [x] 性能检查：上千条库存/Wiki 时搜索和首页统计仍流畅（SQLite 查询基准已通过；Flutter 实机/帧率验证待 SDK 环境补跑）。
- [x] 隐私检查：不上传本地库存，除非用户主动调用自配 VLM endpoint。

验收标准：异常路径不白屏、不丢数据、可定位问题。

## 建议实施顺序

1. **P0 工程/测试/迁移**：先保证改动可验证。
2. **P0 库存闭环 + 提醒**：把 app 最核心价值跑通。
3. **P1 订单识别体验**：减少录入成本。
4. **P5 关键页面 UI 重做**：按 image2 UI 稿逐页复现。
5. **P2 食谱建议 + P3 采购补货**：扩展智能体验。
6. **P4 备份导入导出 + P6 发布**：准备真实长期使用。

## Definition of Done

每个功能 PR 至少满足：

- [x] 有明确用户场景和验收标准。
- [x] UI-heavy 改动附 image2 UI 稿和 Flutter 实现截图（当前已补 Web 实现截图；native 截图待 Android/macOS 发布验收时刷新）。
- [x] 数据结构变更附 migration 和测试。
- [x] 失败/空/加载状态都有处理。
- [x] `flutter analyze` 通过（使用 `.tools/flutter/bin/flutter`）。
- [x] 相关测试通过或说明暂未覆盖原因。
- [x] README/docs 更新。
